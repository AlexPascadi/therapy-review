import React from 'react'
import ReactDOM from 'react-dom'

function RatingTitle(props){
    return(
        <h1 className='rating-title-text'>{props.text}</h1>
    )
}

export default RatingTitle;